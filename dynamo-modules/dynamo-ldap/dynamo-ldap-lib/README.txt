Project provides APIs to interact with an LDAP repository.

1) A sample LDAP file (Sample_LDAP_Dynamo.ldif) is provided with this project. It can be imported into an LDAP directory server like Apache Directory.

2) internal.properties file contain properties that are required to use the LDAP APIs to interact with an LDAP directory server.