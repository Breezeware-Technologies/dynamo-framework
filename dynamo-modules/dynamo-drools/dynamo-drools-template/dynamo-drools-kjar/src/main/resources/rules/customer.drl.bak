package rules;
import com.sample.Customer;
import com.sample.Year;
import com.sample.Customer.CustomerType;

rule "$param"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa1")
		year:Year(experience == 1)
	then
		$customer.setDiscount(400);
		year.setRating(400);
end

// rule values at A10, header at A4
rule "Individual < 3y& Customer with 5 yrs exp"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa2")
		year:Year(experience == 2)
	then
		$customer.setDiscount(5);
		year.setRating(5);
end

// rule values at A11, header at A4
rule "Business Any"
	when
		$customer:Customer(type in ( CustomerType.BUSINESS ), years == "aa3")
		year:Year(experience == 3)
	then
		$customer.setDiscount(20);
		year.setRating(20);
end

// rule values at A12, header at A4
rule "Individual > 3ya"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa4")
		year:Year(experience == 4)
	then
		$customer.setDiscount(406);
		year.setRating(406);
end

// rule values at A13, header at A4
rule "Individual < 3ya"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa5")
		year:Year(experience == 5)
	then
		$customer.setDiscount(5);
		year.setRating(5);
end

// rule values at A14, header at A4
rule "Business Anyaa"
	when
		$customer:Customer(type in ( CustomerType.BUSINESS ), years == "aa6")
		year:Year(experience == 6)
	then
		$customer.setDiscount(20);
		year.setRating(20);
end

// rule values at A15, header at A4
rule "BB"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa7")
		year:Year(experience == 7)
	then
		$customer.setDiscount(406);
		year.setRating(406);
end

// rule values at A16, header at A4
rule "Individual < 3y1"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa8")
		year:Year(experience == 8)
	then
		$customer.setDiscount(5);
		year.setRating(5);
end

// rule values at A17, header at A4
rule "22"
	when
		$customer:Customer(type in ( CustomerType.BUSINESS ), years == "aa9")
		year:Year(experience == 9)
	then
		$customer.setDiscount(20);
		year.setRating(20);
end

// rule values at A18, header at A4
rule "Individual > 3y12"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa10")
		year:Year(experience == 10)
	then
		$customer.setDiscount(406);
		year.setRating(406);
end

// rule values at A19, header at A4
rule "Individual < 3y111"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa11")
		year:Year(experience == 11)
	then
		$customer.setDiscount(5);
		year.setRating(5);
end

// rule values at A20, header at A4
rule "Business Any331111"
	when
		$customer:Customer(type in ( CustomerType.BUSINESS ), years == "aa12")
		year:Year(experience == 12)
	then
		$customer.setDiscount(20);
		year.setRating(20);
end

// rule values at A21, header at A4
rule "Individual > 3y31"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa13")
		year:Year(experience == 13)
	then
		$customer.setDiscount(40611);
		year.setRating(40611);
end

// rule values at A22, header at A4
rule "Individual < 3y32"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa14")
		year:Year(experience == 14)
	then
		$customer.setDiscount(5);
		year.setRating(5);
end

// rule values at A23, header at A4
rule "Business Any22"
	when
		$customer:Customer(type in ( CustomerType.BUSINESS ), years == "aa15")
		year:Year(experience == 15)
	then
		$customer.setDiscount(20);
		year.setRating(20);
end

// rule values at A24, header at A4
rule "Business Any23"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa16")
		year:Year(experience == 16)
	then
		$customer.setDiscount(455);
		year.setRating(455);
end

// rule values at A25, header at A4
rule "Business Any24"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa17")
		year:Year(experience == 17)
	then
		$customer.setDiscount(456);
		year.setRating(456);
end

// rule values at A26, header at A4
rule "Business Any25"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa18")
		year:Year(experience == 18)
	then
		$customer.setDiscount(457);
		year.setRating(457);
end

// rule values at A27, header at A4
rule "Business Any26"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa19")
		year:Year(experience == 19)
	then
		$customer.setDiscount(458);
		year.setRating(458);
end

// rule values at A28, header at A4
rule "Business Any27"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa20")
		year:Year(experience == 20)
	then
		$customer.setDiscount(459);
		year.setRating(459);
end

// rule values at A29, header at A4
rule "Business Any28"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa21")
		year:Year(experience == 21)
	then
		$customer.setDiscount(460);
		year.setRating(460);
end

// rule values at A30, header at A4
rule "Business Any29"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa22")
		year:Year(experience == 22)
	then
		$customer.setDiscount(461);
		year.setRating(461);
end

// rule values at A31, header at A4
rule "Business Any30"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa23")
		year:Year(experience == 23)
	then
		$customer.setDiscount(462);
		year.setRating(462);
end

// rule values at A32, header at A4
rule "Business Any31"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa24")
		year:Year(experience == 24)
	then
		$customer.setDiscount(463);
		year.setRating(463);
end

// rule values at A33, header at A4
rule "Business Any32"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa25")
		year:Year(experience == 25)
	then
		$customer.setDiscount(464);
		year.setRating(464);
end

// rule values at A34, header at A4
rule "Business Any33"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa26")
		year:Year(experience == 26)
	then
		$customer.setDiscount(465);
		year.setRating(465);
end

// rule values at A35, header at A4
rule "Business Any34"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa27")
		year:Year(experience == 27)
	then
		$customer.setDiscount(466);
		year.setRating(466);
end

// rule values at A36, header at A4
rule "Business Any35"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa28")
		year:Year(experience == 28)
	then
		$customer.setDiscount(467);
		year.setRating(467);
end

// rule values at A37, header at A4
rule "Business Any36"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa29")
		year:Year(experience == 29)
	then
		$customer.setDiscount(468);
		year.setRating(468);
end

// rule values at A38, header at A4
rule "Business Any37"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa30")
		year:Year(experience == 30)
	then
		$customer.setDiscount(469);
		year.setRating(469);
end

// rule values at A39, header at A4
rule "Business Any38"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa31")
		year:Year(experience == 31)
	then
		$customer.setDiscount(470);
		year.setRating(470);
end

// rule values at A40, header at A4
rule "Business Any39"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa32")
		year:Year(experience == 32)
	then
		$customer.setDiscount(471);
		year.setRating(471);
end

// rule values at A41, header at A4
rule "Business Any40"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa33")
		year:Year(experience == 33)
	then
		$customer.setDiscount(472);
		year.setRating(472);
end

// rule values at A42, header at A4
rule "Business Any41"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa34")
		year:Year(experience == 34)
	then
		$customer.setDiscount(473);
		year.setRating(473);
end

// rule values at A43, header at A4
rule "Business Any42"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa35")
		year:Year(experience == 35)
	then
		$customer.setDiscount(474);
		year.setRating(474);
end

// rule values at A44, header at A4
rule "Business Any43"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa36")
		year:Year(experience == 36)
	then
		$customer.setDiscount(475);
		year.setRating(475);
end

// rule values at A45, header at A4
rule "Business Any44"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa37")
		year:Year(experience == 37)
	then
		$customer.setDiscount(476);
		year.setRating(476);
end

// rule values at A46, header at A4
rule "Business Any45"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa38")
		year:Year(experience == 38)
	then
		$customer.setDiscount(477);
		year.setRating(477);
end

// rule values at A47, header at A4
rule "Business Any46"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa39")
		year:Year(experience == 39)
	then
		$customer.setDiscount(478);
		year.setRating(478);
end

// rule values at A48, header at A4
rule "Business Any47"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa40")
		year:Year(experience == 40)
	then
		$customer.setDiscount(479);
		year.setRating(479);
end

// rule values at A49, header at A4
rule "Business Any48"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa41")
		year:Year(experience == 41)
	then
		$customer.setDiscount(480);
		year.setRating(480);
end

// rule values at A50, header at A4
rule "Business Any49"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa42")
		year:Year(experience == 42)
	then
		$customer.setDiscount(481);
		year.setRating(481);
end

// rule values at A51, header at A4
rule "Business Any50"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa43")
		year:Year(experience == 43)
	then
		$customer.setDiscount(482);
		year.setRating(482);
end

// rule values at A52, header at A4
rule "Business Any51"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa44")
		year:Year(experience == 44)
	then
		$customer.setDiscount(483);
		year.setRating(483);
end

// rule values at A53, header at A4
rule "Business Any52"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa45")
		year:Year(experience == 45)
	then
		$customer.setDiscount(484);
		year.setRating(484);
end

// rule values at A54, header at A4
rule "Business Any53"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa46")
		year:Year(experience == 46)
	then
		$customer.setDiscount(485);
		year.setRating(485);
end

// rule values at A55, header at A4
rule "Business Any54"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa47")
		year:Year(experience == 47)
	then
		$customer.setDiscount(486);
		year.setRating(486);
end

// rule values at A56, header at A4
rule "Business Any55"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa48")
		year:Year(experience == 48)
	then
		$customer.setDiscount(487);
		year.setRating(487);
end

// rule values at A57, header at A4
rule "Business Any56"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa49")
		year:Year(experience == 49)
	then
		$customer.setDiscount(488);
		year.setRating(488);
end

// rule values at A58, header at A4
rule "Business Any57"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa50")
		year:Year(experience == 50)
	then
		$customer.setDiscount(489);
		year.setRating(489);
end

// rule values at A59, header at A4
rule "Business Any58"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa51")
		year:Year(experience == 51)
	then
		$customer.setDiscount(490);
		year.setRating(490);
end

// rule values at A60, header at A4
rule "Business Any59"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa52")
		year:Year(experience == 52)
	then
		$customer.setDiscount(491);
		year.setRating(491);
end

// rule values at A61, header at A4
rule "Business Any60"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa53")
		year:Year(experience == 53)
	then
		$customer.setDiscount(492);
		year.setRating(492);
end

// rule values at A62, header at A4
rule "Business Any61"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa54")
		year:Year(experience == 54)
	then
		$customer.setDiscount(493);
		year.setRating(493);
end

// rule values at A63, header at A4
rule "Business Any62"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa55")
		year:Year(experience == 55)
	then
		$customer.setDiscount(494);
		year.setRating(494);
end

// rule values at A64, header at A4
rule "Business Any63"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa56")
		year:Year(experience == 56)
	then
		$customer.setDiscount(495);
		year.setRating(495);
end

// rule values at A65, header at A4
rule "Business Any64"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa57")
		year:Year(experience == 57)
	then
		$customer.setDiscount(496);
		year.setRating(496);
end

// rule values at A66, header at A4
rule "Business Any65"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa58")
		year:Year(experience == 58)
	then
		$customer.setDiscount(497);
		year.setRating(497);
end

// rule values at A67, header at A4
rule "Business Any66"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa59")
		year:Year(experience == 59)
	then
		$customer.setDiscount(498);
		year.setRating(498);
end

// rule values at A68, header at A4
rule "Business Any67"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa60")
		year:Year(experience == 60)
	then
		$customer.setDiscount(499);
		year.setRating(499);
end

// rule values at A69, header at A4
rule "Business Any68"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa61")
		year:Year(experience == 61)
	then
		$customer.setDiscount(500);
		year.setRating(500);
end

// rule values at A70, header at A4
rule "Business Any69"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa62")
		year:Year(experience == 62)
	then
		$customer.setDiscount(501);
		year.setRating(501);
end

// rule values at A71, header at A4
rule "Business Any70"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa63")
		year:Year(experience == 63)
	then
		$customer.setDiscount(502);
		year.setRating(502);
end

// rule values at A72, header at A4
rule "Business Any71"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa64")
		year:Year(experience == 64)
	then
		$customer.setDiscount(503);
		year.setRating(503);
end

// rule values at A73, header at A4
rule "Business Any72"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa65")
		year:Year(experience == 65)
	then
		$customer.setDiscount(504);
		year.setRating(504);
end

// rule values at A74, header at A4
rule "Business Any73"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa66")
		year:Year(experience == 66)
	then
		$customer.setDiscount(505);
		year.setRating(505);
end

// rule values at A75, header at A4
rule "Business Any74"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa67")
		year:Year(experience == 67)
	then
		$customer.setDiscount(506);
		year.setRating(506);
end

// rule values at A76, header at A4
rule "Business Any75"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa68")
		year:Year(experience == 68)
	then
		$customer.setDiscount(507);
		year.setRating(507);
end

// rule values at A77, header at A4
rule "Business Any76"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa69")
		year:Year(experience == 69)
	then
		$customer.setDiscount(508);
		year.setRating(508);
end

// rule values at A78, header at A4
rule "Business Any77"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa70")
		year:Year(experience == 70)
	then
		$customer.setDiscount(509);
		year.setRating(509);
end

// rule values at A79, header at A4
rule "Business Any78"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa71")
		year:Year(experience == 71)
	then
		$customer.setDiscount(510);
		year.setRating(510);
end

// rule values at A80, header at A4
rule "Business Any79"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa72")
		year:Year(experience == 72)
	then
		$customer.setDiscount(511);
		year.setRating(511);
end

// rule values at A81, header at A4
rule "Business Any80"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa73")
		year:Year(experience == 73)
	then
		$customer.setDiscount(512);
		year.setRating(512);
end

// rule values at A82, header at A4
rule "Business Any81"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa74")
		year:Year(experience == 74)
	then
		$customer.setDiscount(513);
		year.setRating(513);
end

// rule values at A83, header at A4
rule "Business Any82"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa75")
		year:Year(experience == 75)
	then
		$customer.setDiscount(514);
		year.setRating(514);
end

// rule values at A84, header at A4
rule "Business Any83"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa76")
		year:Year(experience == 76)
	then
		$customer.setDiscount(515);
		year.setRating(515);
end

// rule values at A85, header at A4
rule "Business Any84"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa77")
		year:Year(experience == 77)
	then
		$customer.setDiscount(516);
		year.setRating(516);
end

// rule values at A86, header at A4
rule "Business Any85"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa78")
		year:Year(experience == 78)
	then
		$customer.setDiscount(517);
		year.setRating(517);
end

// rule values at A87, header at A4
rule "Business Any86"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa79")
		year:Year(experience == 79)
	then
		$customer.setDiscount(518);
		year.setRating(518);
end

// rule values at A88, header at A4
rule "Business Any87"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa80")
		year:Year(experience == 80)
	then
		$customer.setDiscount(519);
		year.setRating(519);
end

// rule values at A89, header at A4
rule "Business Any88"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa81")
		year:Year(experience == 81)
	then
		$customer.setDiscount(520);
		year.setRating(520);
end

// rule values at A90, header at A4
rule "Business Any89"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa82")
		year:Year(experience == 82)
	then
		$customer.setDiscount(521);
		year.setRating(521);
end

// rule values at A91, header at A4
rule "Business Any90"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa83")
		year:Year(experience == 83)
	then
		$customer.setDiscount(522);
		year.setRating(522);
end

// rule values at A92, header at A4
rule "Business Any91"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa84")
		year:Year(experience == 84)
	then
		$customer.setDiscount(523);
		year.setRating(523);
end

// rule values at A93, header at A4
rule "Business Any92"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa85")
		year:Year(experience == 85)
	then
		$customer.setDiscount(524);
		year.setRating(524);
end

// rule values at A94, header at A4
rule "Business Any93"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa86")
		year:Year(experience == 86)
	then
		$customer.setDiscount(525);
		year.setRating(525);
end

// rule values at A95, header at A4
rule "Business Any94"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa87")
		year:Year(experience == 87)
	then
		$customer.setDiscount(526);
		year.setRating(526);
end

// rule values at A96, header at A4
rule "Business Any95"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa88")
		year:Year(experience == 88)
	then
		$customer.setDiscount(527);
		year.setRating(527);
end

// rule values at A97, header at A4
rule "Business Any96"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa89")
		year:Year(experience == 89)
	then
		$customer.setDiscount(528);
		year.setRating(528);
end

// rule values at A98, header at A4
rule "Business Any97"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa90")
		year:Year(experience == 90)
	then
		$customer.setDiscount(529);
		year.setRating(529);
end

// rule values at A99, header at A4
rule "Business Any98"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa91")
		year:Year(experience == 91)
	then
		$customer.setDiscount(530);
		year.setRating(530);
end

// rule values at A100, header at A4
rule "Business Any99"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa92")
		year:Year(experience == 92)
	then
		$customer.setDiscount(531);
		year.setRating(531);
end

// rule values at A101, header at A4
rule "Business Any100"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa93")
		year:Year(experience == 93)
	then
		$customer.setDiscount(532);
		year.setRating(532);
end

// rule values at A102, header at A4
rule "Business Any101"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa94")
		year:Year(experience == 94)
	then
		$customer.setDiscount(533);
		year.setRating(533);
end

// rule values at A103, header at A4
rule "Business Any102"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa95")
		year:Year(experience == 95)
	then
		$customer.setDiscount(534);
		year.setRating(534);
end

// rule values at A104, header at A4
rule "Business Any103"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa96")
		year:Year(experience == 96)
	then
		$customer.setDiscount(535);
		year.setRating(535);
end

// rule values at A105, header at A4
rule "Business Any104"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa97")
		year:Year(experience == 97)
	then
		$customer.setDiscount(536);
		year.setRating(536);
end

// rule values at A106, header at A4
rule "Business Any105"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa98")
		year:Year(experience == 98)
	then
		$customer.setDiscount(537);
		year.setRating(537);
end

// rule values at A107, header at A4
rule "Business Any106"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa99")
		year:Year(experience == 99)
	then
		$customer.setDiscount(538);
		year.setRating(538);
end

// rule values at A108, header at A4
rule "Business Any107"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa100")
		year:Year(experience == 100)
	then
		$customer.setDiscount(539);
		year.setRating(539);
end

// rule values at A109, header at A4
rule "Business Any108"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa101")
		year:Year(experience == 101)
	then
		$customer.setDiscount(540);
		year.setRating(540);
end

// rule values at A110, header at A4
rule "Business Any109"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa102")
		year:Year(experience == 102)
	then
		$customer.setDiscount(541);
		year.setRating(541);
end

// rule values at A111, header at A4
rule "Business Any110"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa103")
		year:Year(experience == 103)
	then
		$customer.setDiscount(542);
		year.setRating(542);
end

// rule values at A112, header at A4
rule "Business Any111"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa104")
		year:Year(experience == 104)
	then
		$customer.setDiscount(543);
		year.setRating(543);
end

// rule values at A113, header at A4
rule "Business Any112"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa105")
		year:Year(experience == 105)
	then
		$customer.setDiscount(544);
		year.setRating(544);
end

// rule values at A114, header at A4
rule "Business Any113"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa106")
		year:Year(experience == 106)
	then
		$customer.setDiscount(545);
		year.setRating(545);
end

// rule values at A115, header at A4
rule "Business Any114"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa107")
		year:Year(experience == 107)
	then
		$customer.setDiscount(546);
		year.setRating(546);
end

// rule values at A116, header at A4
rule "Business Any115"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa108")
		year:Year(experience == 108)
	then
		$customer.setDiscount(547);
		year.setRating(547);
end

// rule values at A117, header at A4
rule "Business Any116"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa109")
		year:Year(experience == 109)
	then
		$customer.setDiscount(548);
		year.setRating(548);
end

// rule values at A118, header at A4
rule "Business Any117"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa110")
		year:Year(experience == 110)
	then
		$customer.setDiscount(549);
		year.setRating(549);
end

// rule values at A119, header at A4
rule "Business Any118"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa111")
		year:Year(experience == 111)
	then
		$customer.setDiscount(550);
		year.setRating(550);
end

// rule values at A120, header at A4
rule "Business Any119"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa112")
		year:Year(experience == 112)
	then
		$customer.setDiscount(551);
		year.setRating(551);
end

// rule values at A121, header at A4
rule "Business Any120"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa113")
		year:Year(experience == 113)
	then
		$customer.setDiscount(552);
		year.setRating(552);
end

// rule values at A122, header at A4
rule "Business Any121"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa114")
		year:Year(experience == 114)
	then
		$customer.setDiscount(553);
		year.setRating(553);
end

// rule values at A123, header at A4
rule "Business Any122"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa115")
		year:Year(experience == 115)
	then
		$customer.setDiscount(554);
		year.setRating(554);
end

// rule values at A124, header at A4
rule "Business Any123"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa116")
		year:Year(experience == 116)
	then
		$customer.setDiscount(555);
		year.setRating(555);
end

// rule values at A125, header at A4
rule "Business Any124"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa117")
		year:Year(experience == 117)
	then
		$customer.setDiscount(556);
		year.setRating(556);
end

// rule values at A126, header at A4
rule "Business Any125"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa118")
		year:Year(experience == 118)
	then
		$customer.setDiscount(557);
		year.setRating(557);
end

// rule values at A127, header at A4
rule "Business Any126"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa119")
		year:Year(experience == 119)
	then
		$customer.setDiscount(558);
		year.setRating(558);
end

// rule values at A128, header at A4
rule "Business Any127"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa120")
		year:Year(experience == 120)
	then
		$customer.setDiscount(559);
		year.setRating(559);
end

// rule values at A129, header at A4
rule "Business Any128"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa121")
		year:Year(experience == 121)
	then
		$customer.setDiscount(560);
		year.setRating(560);
end

// rule values at A130, header at A4
rule "Business Any129"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa122")
		year:Year(experience == 122)
	then
		$customer.setDiscount(561);
		year.setRating(561);
end

// rule values at A131, header at A4
rule "Business Any130"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa123")
		year:Year(experience == 123)
	then
		$customer.setDiscount(562);
		year.setRating(562);
end

// rule values at A132, header at A4
rule "Business Any131"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa124")
		year:Year(experience == 124)
	then
		$customer.setDiscount(563);
		year.setRating(563);
end

// rule values at A133, header at A4
rule "Business Any132"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa125")
		year:Year(experience == 125)
	then
		$customer.setDiscount(564);
		year.setRating(564);
end

// rule values at A134, header at A4
rule "Business Any133"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa126")
		year:Year(experience == 126)
	then
		$customer.setDiscount(565);
		year.setRating(565);
end

// rule values at A135, header at A4
rule "Business Any134"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa127")
		year:Year(experience == 127)
	then
		$customer.setDiscount(566);
		year.setRating(566);
end

// rule values at A136, header at A4
rule "Business Any135"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa128")
		year:Year(experience == 128)
	then
		$customer.setDiscount(567);
		year.setRating(567);
end

// rule values at A137, header at A4
rule "Business Any136"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa129")
		year:Year(experience == 129)
	then
		$customer.setDiscount(568);
		year.setRating(568);
end

// rule values at A138, header at A4
rule "Business Any137"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa130")
		year:Year(experience == 130)
	then
		$customer.setDiscount(569);
		year.setRating(569);
end

// rule values at A139, header at A4
rule "Business Any138"
	when
		$customer:Customer(type in ( CustomerType.INDIVIDUAL ), years == "aa131")
		year:Year(experience == 131)
	then
		$customer.setDiscount(570);
		year.setRating(570);
end